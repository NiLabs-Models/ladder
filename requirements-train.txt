# Installed only on the GPU box (Kaggle / Colab / rented T4-or-better).
#
# Deliberately NOT pinned. An earlier version pinned unsloth==2025.9.1 "for
# determinism", and that pin is what broke the second run: Kaggle's image ships
# transformers 5.0.0, which that unsloth predates so completely that it cannot
# import (`NameError: PreTrainedConfig`). A pin chosen without checking it
# against the target environment is not determinism, it is a guess with a
# version number on it.
#
# unsloth resolves its own compatible transformers/trl/peft, so let it. If a
# future run needs a frozen set, freeze one that has actually been observed
# working -- see results/*/run_config.json for what a given run really used.
unsloth
unsloth_zoo
bitsandbytes>=0.43.0

# Used by data prep and the eval harness, independent of the training stack.
datasets>=2.19
huggingface_hub>=0.24
pyyaml>=6.0
